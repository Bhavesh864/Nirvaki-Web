import 'package:flutter/material.dart';

class OnFinishInventoryQuestion extends StatelessWidget {
  const OnFinishInventoryQuestion({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
